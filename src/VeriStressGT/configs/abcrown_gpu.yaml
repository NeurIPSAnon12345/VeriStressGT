general:
  device: cuda
  save_adv_example: true          # keep counterexamples so SAT verdicts can be re-checked in float64
attack:
  pgd_order: input_bab
  cex_path: ./test_cex.txt
solver:
  alpha-crown:
    max_time: 1000000000.0
bab:
  timeout: 3000000.0
